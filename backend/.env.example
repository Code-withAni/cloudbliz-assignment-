# Server port
PORT=3000

# MongoDB connection string
MONGO_URI=mongodb://127.0.0.1:27017/cloudblitz_enquiry_management

# JWT signing secret (use a long random string in production)
JWT_SECRET=change-me-to-a-long-random-secret